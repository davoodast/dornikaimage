# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files — never commit
.env
.env.local
.env*.local

# project-specific temp dirs
/uploads/
/compressed/
/data/

# PWA generated files
/public/sw.js
/public/workbox-*.js

# local font — track in repo (no CDN allowed)
# /public/fonts/ — DO NOT ignore; fonts are local-only assets

# misc
.DS_Store
*.pem
npm-debug.log*
